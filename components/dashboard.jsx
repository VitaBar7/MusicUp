import React from "react"
import useAuth from '@component/utils/useAuth'

/* export const Dashboard = ({ code }) => {
    const accessToken = useAuth(code)
    return (
        <>
         <div>{code}</div>
        </>
    )
}


export default Dashboard */